import { InternalAjaxRequestOptions } from '../types';
import FilterChain from '../FilterChain';
export default function pathVariableFilter(options: InternalAjaxRequestOptions, chain: FilterChain): any;
